<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="com/sakanal/MyBatis/C_Mapper/properties/jdbc.properties"/>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!--    显示的指定我们需要更改的配置的值，即使它是默认的。防止版本更新带来的问题    -->
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="aggressiveLazyLoading" value="false"/>
    </settings>
    <typeAliases>
        <package name="com.sakanal.MyBatis.C_Mapper.bean"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driverClassName}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="com/sakanal/MyBatis/C_Mapper/dao/UserMapper.xml"/>
        <mapper resource="com/sakanal/MyBatis/C_Mapper/dao/UserMapperPlus.xml"/>
        <mapper resource="com/sakanal/MyBatis/C_Mapper/dao/DepartmentMapper.xml"/>
<!--    批量注册时要将.java和.xml文件放一起    -->
<!--        <package name="com.sakanal.MyBatis.C_Mapper.mapper"/>-->
    </mappers>
</configuration>
