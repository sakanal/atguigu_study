<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sakanal.MyBatis.F_SSM.dao.UserMapper">

    <resultMap id="MyUser" type="com.sakanal.MyBatis.F_SSM.pojo.User">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="gender" property="gender"/>
        <result column="address" property="address"/>
        <result column="phone" property="phone"/>
        <association property="department"
                     select="com.sakanal.MyBatis.F_SSM.dao.DepartmentMapper.getDepartmentById"
                     column="department_id">
        </association>
    </resultMap>
    <select id="getUserById" resultMap="MyUser">
        select * from `user` where id=#{id}
    </select>
    <select id="getUser" resultType="com.sakanal.MyBatis.F_SSM.pojo.User">
        select <include refid="userInfo"/> from `user`
    </select>
    <sql id="userInfo">
        id,name,password,gender,address,phone
    </sql>
</mapper>