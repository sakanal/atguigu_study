server:
  port: 3366
spring:
  application:
    name: cloud-config-client
  cloud:
    config:
      uri: http://localhost:3344
      label: main
      name: config
      profile: test
  rabbitmq:
    username: guest
    password: guest
    host: localhost
    port: 5672
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
